pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

plugins {
    id 'io.micronaut.build.shared.settings' version '6.1.1'
}

rootProject.name = 'flyway-parent'

include 'flyway'
include 'flyway-bom'
