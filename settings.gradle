pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

plugins {
    id("io.micronaut.build.shared.settings") version "4.2.2"
}

rootProject.name = 'micronaut-flyway'

include 'flyway'
