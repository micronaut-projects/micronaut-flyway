pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

plugins {
    id 'io.micronaut.build.shared.settings' version '5.0.2'
}

rootProject.name = 'flyway-parent'

include 'flyway'
